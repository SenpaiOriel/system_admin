# Bridge: backend connection (load with dotenv in Node)
# Backend reads these; use same values in frontend/config.js API_BASE_URL if frontend runs separately.

HOST=localhost
PORT=3000
NODE_ENV=development

# Full URL where the backend API is reachable (for CORS and frontend calls)
API_BASE_URL=http://localhost:3000

# Allowed origin for browser requests (use * in dev only if needed)
CORS_ORIGIN=http://localhost:3000
